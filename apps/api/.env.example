# Optional: used by scripts/test-identity.ts.
# Database config is sourced from @chris-test/db (packages/db/.env).
API_BASE=http://localhost:3200

# Optional feature flag for Graph read routes.
# Set to "false" to disable /graph/v1/statements|entities|search.
GRAPH_API_V1_ENABLED=true
